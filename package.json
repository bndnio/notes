{
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "logs": "wrangler tail",
    "secret-sender": "wrangler secret put ALLOWED_SENDER",
    "secret-notion-token": "wrangler secret put NOTION_TOKEN",
    "secret-notion-db": "wrangler secret put NOTION_DB_ID",
    "bucket": "wrangler r2 bucket create bndnio-notes"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "wrangler": "^4.90.1"
  }
}